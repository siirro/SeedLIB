<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.seed.lib.studyroom.LockerMapper">
	
	<select id="getLockerCount" parameterType="String" resultType="LockerVO">
		SELECT * FROM 
		LOCKER L
		LEFT JOIN
		LOCKERRENT LR
		ON L.LOCKERLAST=LR.LOCKERLAST	
	</select>
	
	<select id="getUseLocker" parameterType="LockerVO" resultType="int">
		SELECT COUNT(*) FROM
		LOCKERRENT
		WHERE LOCKERNUM=#{lockerNum} AND RENTSTAT=0
	</select>
	
	<select id="getOverlapLocker" parameterType="LockerVO" resultType="int">
		SELECT COUNT(*) FROM
		LOCKERRENT
		WHERE USERNAME=#{userName} AND RENTSTAT=0
	</select>
	
	<insert id="setMyLocker" parameterType="LockerVO">
		INSERT INTO LOCKERRENT
		(LOCKERNUM, PRICE, STDATE, ENDATE, RENTDAYS, RENTSTAT, USERNAME, LOCKERLAST)
		VALUES(#{lockerNum}, #{price}, #{stDate}, #{enDate}, #{rentDays}, 0, #{userName}, 40);
	</insert>
	
	<!-- <select id="getSeatHistory" parameterType="String" resultType="StudyDetailVO">
		SELECT * FROM STUDYDETAIL 
		WHERE USERNAME=#{userName}
	</select>
	
	<update id="exitMySeat" parameterType="StudyDetailVO">
		UPDATE STUDYDETAIL SET
		RVABLE=1
		WHERE USERNAME=#{userName} AND SEATNUM=#{seatNum};
	</update> 
	
	<resultMap type="LockerVO" id="getLockerResult">
		<id column="RVNUM" property="rvNum"/>
		<result column="RVABLE" property="rvAble"/>
		<result column="SEATNUM" property="seatNum"/>
		<result column="RVDATE" property="rvDate"/>  
		<result column="USERNAME" property="userName"/>
		<association property="roomVO">
			<id column="ROOMNUM" property="roomNum"/>
			<result column="ROOMNAME" property="roomName"/>
			<result column="LASTNUM" property="lastNum"/>
		</association>
	</resultMap> -->
</mapper>    
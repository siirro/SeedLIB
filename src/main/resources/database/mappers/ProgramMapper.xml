<?xml version="1.0" encoding="UTF-8"?>
		<!DOCTYPE mapper
		    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		    "http://www.mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.seed.lib.admin.program.ProgramMapper">
	<insert id="setProgramAdd" parameterType="ProgramVO">
		INSERT INTO PROGRAM (PRONUM, PRONAME, TOTAL, PRSDATE, PRLDATE, PSDATE, PLDATE, TARGET, PROTEACHER, PROPLACE, PROCATE)
		VALUES (null, #{proName}, #{total}, #{prsDate}, #{prlDate}, #{psDate}, #{plDate}, #{target}, #{proTeacher}, #{proPlace}, #{proCate})
	</insert>
</mapper>